# Dashboard design replication

This is a code bundle for Dashboard design replication. The original project is available at https://www.figma.com/design/gAHY4HhUrhPEtN3eeeGBcE/Dashboard-design-replication.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
